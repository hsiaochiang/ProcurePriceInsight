# Google Colab PoC 規劃說明：採購單價預測 / 詢價參考模型

## 1. PoC 目標

本次 PoC 的目的不是直接建立正式可上線的採購報價系統，而是驗證以下問題：

1. 現有採購資料是否足以支撐機器學習建模。
2. 品名中的規格資訊是否能被穩定解析成可用欄位。
3. 模型是否能比簡單歷史價格基準更準確。
4. 模型是否能提供可被採購人員理解的價格區間與相似案例。
5. 本案是否值得進入下一階段 MVP 或正式導入。

PoC 的建議定位為：

> 建立「詢價參考區間 + 相似案例比價 + 異常價格提醒」的原型，而不是追求一次到位的精準單點價格預測。

## 2. PoC 執行環境

### 建議平台

- Google Colab
- Python 3.x
- Notebook 格式：`.ipynb`

### 建議套件

Colab 第一個 cell 安裝或載入以下套件：

```python
!pip install pandas numpy openpyxl scikit-learn lightgbm xgboost sentence-transformers matplotlib seaborn
```

必要套件用途：

| 套件 | 用途 |
|---|---|
| pandas | 讀取 Excel、資料清洗 |
| numpy | 數值處理 |
| openpyxl | 讀取 `.xlsx` |
| scikit-learn | baseline、模型評估、資料切分 |
| lightgbm / xgboost | 回歸模型 |
| sentence-transformers | 品名 embedding 與相似案例檢索 |
| matplotlib / seaborn | 視覺化 |

## 3. PoC 輸入資料

### 必要資料

- `採購DataE02_POC.xlsx`

### 必要欄位

| 欄位 | 說明 | PoC 是否必要 |
|---|---|---|
| 採購日期 | 用於時間切分與時間趨勢分析 | 是 |
| 幣別 | 判斷是否需排除或轉換外幣 | 是 |
| 單價 | 預測目標欄位 | 是 |
| 數量 | 判斷採購量對單價影響 | 是 |
| 料號 | 可用於相似案例，但需避免資料洩漏 | 是 |
| 品名 | 核心特徵來源 | 是 |
| 廠商編號 | 可作價格條件，但需謹慎使用 | 是 |

### 欄位標準化要求

Excel 欄位可能含全形空白，例如：

- `單　價`
- `數　量`

PoC 必須先標準化欄位名稱：

```python
df.columns = [
    str(c).replace("\u3000", "").replace("　", "").strip()
    for c in df.columns
]
```

## 4. PoC Notebook 建議章節

Colab Notebook 建議依下列章節執行，避免只做模型訓練而缺少資料與業務驗證。

### 章節 1：環境與資料讀取

執行內容：

1. 安裝必要套件。
2. 上傳 Excel 檔案。
3. 讀取所有工作表。
4. 檢查欄位名稱。
5. 標準化欄位名稱。

輸出結果：

- 工作表名稱。
- 資料筆數與欄位數。
- 原始欄位名稱與標準化後欄位名稱。
- 前 10 筆資料預覽。

通過條件：

- 能成功讀取 Excel。
- 必要欄位皆存在。
- `單價`、`數量`、`採購日期` 可轉成正確資料型態。

未通過處理：

- 若必要欄位缺失，PoC 暫停，需回客戶補資料。

### 章節 2：資料品質檢查

執行內容：

1. 檢查缺失值。
2. 檢查完全重複列。
3. 檢查 `單價 <= 0`。
4. 檢查 `數量 <= 0`。
5. 檢查幣別分布。
6. 檢查採購日期範圍。
7. 檢查單價與數量分布。
8. 檢查料號、品名、廠商編號唯一率。

輸出結果：

| 檢查項目 | 輸出 |
|---|---|
| 缺失值 | 每欄缺失筆數與比例 |
| 完全重複列 | 重複筆數 |
| 單價異常 | `單價 <= 0` 筆數 |
| 數量異常 | `數量 <= 0` 筆數 |
| 幣別 | 各幣別筆數 |
| 日期 | 最早與最晚採購日期 |
| 單價分布 | min、P25、P50、P75、P95、P99、max |
| 唯一率 | 料號、品名、廠商編號唯一率 |

判斷條件：

| 項目 | 可接受條件 | 若不符合 |
|---|---|---|
| 必要欄位缺失 | 0 | 停止 PoC，需補資料 |
| 單價 <= 0 | 應為 0 或可明確排除 | 排除或回客戶確認 |
| 數量 <= 0 | 需低於 10%，或可清楚解釋 | 若無法解釋，模型結果不得作正式判斷 |
| 外幣資料 | 須有匯率，否則先排除 | PoC 第一版只用台幣 |
| 品名缺失 | 應低於 5% | 若過高，品名解析不可行 |

本案已知狀態：

- 缺失值：0。
- 完全重複列：19。
- 單價 <= 0：0。
- 數量 <= 0：489。
- 幣別：台幣 3,061 筆，美金 29 筆。

因此 PoC 第一版建議使用：

```text
幣別 = 台幣
單價 > 0
數量 > 0
去除完全重複列
```

### 章節 3：品名解析與特徵抽取

執行內容：

從 `品名` 欄位解析以下特徵：

| 特徵 | 範例 | 方法 |
|---|---|---|
| 品項類別 | 動力分電盤、受電箱、PLC盤 | 取第一個逗號前文字 |
| W 尺寸 | 800mm(W) | regex |
| H 尺寸 | 2100mm(H) | regex |
| D 尺寸 | 500mm(D) | regex |
| 體積 | W x H x D | 計算 |
| 表面積 proxy | W x H、W x D、H x D | 計算 |
| 板厚 | 2.0t、3.2/2.3t | regex |
| 材質 | SPHC、SS41、SUS304、黑鐵 | regex |
| 室內 / 屋內 | 屋內型、室內 | keyword |
| 室外 / 屋外 | 屋外型、室外 | keyword |
| 防水 | 防水 | keyword |
| 防塵 | 防塵 | keyword |
| 安裝型式 | 壁掛式、落地式、直立式 | keyword |
| 附件 | 變壓器、變頻器、NFB、ACB、PLC | keyword |

輸出結果：

- 每個特徵的解析成功率。
- 解析失敗樣本清單。
- 解析後資料表前 20 筆。
- 主要品項類別統計。
- 材質統計。

品名解析判斷條件：

| 指標 | Go 條件 | Watch 條件 | No-Go 條件 |
|---|---:|---:|---:|
| 尺寸解析率 | >= 70% | 50%-70% | < 50% |
| 板厚解析率 | >= 70% | 50%-70% | < 50% |
| 材質解析率 | >= 70% | 50%-70% | < 50% |
| 品項類別解析率 | >= 95% | 80%-95% | < 80% |

本案已知初步結果：

- W/H/D 尺寸覆蓋率約 79.42%。
- 板厚覆蓋率約 78.71%。
- 材質覆蓋率約 78.64%。

因此品名解析具備 PoC 條件。

### 章節 4：資料切分設計

本案不可只使用隨機切分，因為採購價格有時間趨勢，且同一料號或相似品名可能造成資料洩漏。

PoC 至少執行兩種切分：

#### 切分 A：時間切分

建議：

```text
訓練集：2018-01-01 至 2024-12-31
測試集：2025-01-01 至 2026-03-06
```

用途：

- 模擬真實情境：用過去資料預測未來採購價格。

#### 切分 B：料號隔離切分

要求：

- 同一料號不可同時出現在訓練集與測試集。
- 若資料量不足，可改用同一完整品名不可同時出現在訓練集與測試集。

用途：

- 檢查模型是否只是記憶歷史料號價格。

判斷條件：

| 切分方式 | 必須執行 | 用途 |
|---|---|---|
| 時間切分 | 是 | 驗證未來預測能力 |
| 料號隔離切分 | 是 | 驗證泛化能力 |
| 隨機切分 | 可選 | 僅作參考，不可作為主要驗收 |

### 章節 5：建立 Baseline

模型必須與 baseline 比較，否則無法判斷機器學習是否真的有價值。

至少建立以下 baseline：

| Baseline | 說明 |
|---|---|
| Global Median | 全部訓練資料的單價中位數 |
| Category Median | 同品項類別的歷史單價中位數 |
| Category + Material Median | 同類別、同材質的歷史中位數 |
| Similar Spec Median | 相似尺寸、類別、材質的歷史中位數 |

其中 `Category Median` 是最低可接受基準：

> 只看品項類別，例如動力分電盤、受電箱、分電箱，取該類別歷史單價中位數作為預測。

判斷條件：

- 若 ML 模型無法明顯優於 `Category Median`，不建議進入 MVP。
- 若 ML 模型只在隨機切分勝出，但在時間切分失敗，不建議進入 MVP。

### 章節 6：模型訓練

PoC 建議訓練三類模型。

#### 模型 1：規則特徵 + 傳統 ML 回歸

建議模型：

- Random Forest Regressor
- XGBoost Regressor
- LightGBM Regressor

目標值建議：

```text
y = log1p(單價)
```

預測後轉回原始價格：

```python
pred_price = np.expm1(pred_log_price)
```

特徵建議：

- 品項類別
- W / H / D
- 體積
- 表面積 proxy
- 板厚
- 材質
- 室內 / 室外
- 防水 / 防塵
- 安裝型式
- 是否含 NFB / ACB / PLC / 變壓器
- 採購年份 / 月份
- 數量
- 廠商編號，可做對照實驗，不宜一開始過度依賴

#### 模型 2：品名文字向量 + 回歸

方法：

- 使用 TF-IDF 或 sentence embedding 將品名轉成向量。
- 搭配 Ridge、Random Forest、LightGBM 或 XGBoost。

用途：

- 補足規則解析漏掉的文字資訊。

注意：

- 不可只靠完整品名 embedding 得到高分就判定成功，因為可能仍有記憶效果。

#### 模型 3：相似案例檢索

方法：

- 使用結構化特徵距離。
- 或使用品名 embedding cosine similarity。
- 找出 Top 5 / Top 10 相似歷史採購紀錄。

輸出：

- 相似品名。
- 歷史單價。
- 採購日期。
- 廠商。
- 尺寸 / 材質 / 板厚差異。
- 相似度分數。

用途：

- 作為採購議價依據。
- 作為模型解釋來源。

### 章節 7：評估指標

PoC 不可只看單一指標。建議同時使用以下指標：

| 指標 | 說明 |
|---|---|
| MAE | 平均絕對誤差，單位為價格 |
| RMSE | 對高價錯誤更敏感 |
| MAPE | 平均百分比誤差，但會受低價品影響 |
| Median APE | 百分比誤差中位數，較不受極端值影響 |
| WAPE | 加權百分比誤差，適合總體採購金額 |
| Within ±20% | 預測落在實際價格上下 20% 內的比例 |
| Within ±30% | 預測落在實際價格上下 30% 內的比例 |
| Within ±50% | 預測落在實際價格上下 50% 內的比例 |
| Prediction Interval Coverage | 實際價格是否落在模型預測區間內 |

### 指標計算定義

#### APE

```text
APE = abs(預測價格 - 實際價格) / 實際價格
```

#### Median APE

```text
所有 APE 的中位數
```

#### Within ±30%

```text
APE <= 30% 的筆數 / 測試集總筆數
```

#### WAPE

```text
WAPE = sum(abs(預測價格 - 實際價格)) / sum(實際價格)
```

## 5. 明確 Go / No-Go 判斷指標

本 PoC 的核心是決定是否繼續投資。建議使用下列判斷表。

### 5.1 資料品質 Go / No-Go

| 指標 | Go | Watch | No-Go |
|---|---:|---:|---:|
| 必要欄位完整性 | 100% | - | 任一必要欄位缺失 |
| 單價有效率 | >= 98% | 95%-98% | < 95% |
| 品名有效率 | >= 95% | 80%-95% | < 80% |
| 台幣資料占比 | >= 90% 或有匯率 | 70%-90% | < 70% 且無匯率 |
| 數量異常可解釋性 | 可清楚處理 | 部分可處理 | 無法解釋且比例高 |

本案初步判斷：

- 欄位完整性：Go。
- 單價有效率：Go。
- 品名有效率：Go。
- 幣別：Go，但第一版應排除美金。
- 數量異常：Watch，需客戶解釋 `數量 = 0` 的業務意義。

### 5.2 品名解析 Go / No-Go

| 指標 | Go | Watch | No-Go |
|---|---:|---:|---:|
| 品項類別解析率 | >= 95% | 80%-95% | < 80% |
| 尺寸解析率 | >= 70% | 50%-70% | < 50% |
| 材質解析率 | >= 70% | 50%-70% | < 50% |
| 板厚解析率 | >= 70% | 50%-70% | < 50% |
| 解析後可用訓練筆數 | >= 2,000 | 1,000-2,000 | < 1,000 |

本案初步判斷：

- 尺寸解析率約 79.42%。
- 材質解析率約 78.64%。
- 板厚解析率約 78.71%。

因此品名解析可進入 PoC。

### 5.3 模型效果 Go / No-Go

以下指標以「時間切分測試集」為主要驗收標準。

#### 最低通過條件

| 指標 | 最低通過條件 |
|---|---|
| ML Median APE | 必須低於 Category Median baseline 至少 15% 相對改善 |
| ML Within ±30% | 必須高於 Category Median baseline 至少 10 個百分點 |
| ML Within ±50% | 必須高於 Category Median baseline 至少 10 個百分點 |
| 高價品項錯誤 | 不得大量低估高價品，需有風險提示 |
| 相似案例可用性 | Top 5 相似案例中至少 3 筆需被業務認為合理 |

#### 建議進入 MVP 條件

| 指標 | 建議門檻 |
|---|---:|
| Median APE | <= 35% |
| Within ±30% | >= 45% |
| Within ±50% | >= 65% |
| Prediction Interval Coverage | >= 75% |
| 相似案例業務接受率 | >= 70% |
| 對 baseline 相對改善 | >= 20% |

#### 只適合繼續研究、不建議進 MVP 的條件

| 情況 | 判斷 |
|---|---|
| Median APE > 50% | 單點預測價值不足 |
| Within ±30% < 35% | 價格接近度不足 |
| 只比 baseline 好一點點 | 商業價值不明顯 |
| 只能在隨機切分表現好 | 有資料洩漏風險 |
| 高價品項嚴重低估 | 不適合作為議價依據 |

#### 建議停止或重整資料的條件

| 情況 | 判斷 |
|---|---|
| ML 模型無法優於 Category Median baseline | 暫不值得導入模型 |
| 品名解析率低於 50% | 需改用人工規格表或 BOM |
| 測試集表現極不穩定 | 需補資料或重新定義品項範圍 |
| 業務無法接受相似案例結果 | 不適合進入 MVP |
| 客戶無法補充關鍵採購條件 | 只能做粗略參考，不宜正式導入 |

## 6. PoC 詳細執行步驟

### Step 1：上傳資料

在 Colab 上傳：

```text
採購DataE02_POC.xlsx
```

輸出：

- 檔案成功讀取。
- 顯示工作表與欄位。

完成條件：

- Colab 可成功讀取 Excel。

### Step 2：欄位標準化與型態轉換

處理：

- 去除欄位全形空白。
- `採購日期` 轉 datetime。
- `單價`、`數量` 轉 numeric。

完成條件：

- 必要欄位無缺。
- 型態轉換錯誤筆數可列出。

### Step 3：資料品質報告

產出：

- 缺失值表。
- 重複列數。
- 幣別統計。
- 單價分布。
- 數量分布。
- 日期分布。
- 料號 / 品名唯一率。

完成條件：

- 產出可讀表格與圖表。
- 明確標示資料排除規則。

### Step 4：建立清洗後資料集

PoC 第一版建議規則：

```text
保留 幣別 = 台幣
保留 單價 > 0
保留 數量 > 0
移除完全重複列
```

輸出：

- 原始資料筆數。
- 清洗後資料筆數。
- 被排除資料原因統計。

完成條件：

- 清洗後資料 >= 2,000 筆，建議才繼續建模。

### Step 5：品名解析

建立 regex 或規則函式，抽取：

- 類別。
- W/H/D。
- 體積。
- 板厚。
- 材質。
- 室內外。
- 防水防塵。
- 安裝型式。
- 附件與特殊關鍵字。

輸出：

- 解析成功率。
- 解析失敗樣本。
- 特徵統計表。

完成條件：

- 尺寸、材質、板厚解析率達 Go 或 Watch。
- 若低於 No-Go，需先改善解析規則。

### Step 6：EDA 與價格關係檢查

分析：

- 不同品項類別的價格分布。
- 尺寸 / 體積與價格關係。
- 材質與價格關係。
- 防水防塵與價格關係。
- 廠商與價格關係。
- 年度價格趨勢。

輸出：

- 箱型圖。
- 散佈圖。
- 年度中位數價格。
- 高價 Top 20 品項清單。

完成條件：

- 可說明哪些特徵與價格可能有關。
- 可識別高價長尾是否需要分段處理。

### Step 7：資料切分

至少建立：

1. 時間切分資料集。
2. 料號隔離資料集。

輸出：

- 訓練集筆數。
- 測試集筆數。
- 測試集日期範圍。
- 訓練 / 測試是否有料號重疊。

完成條件：

- 時間切分測試集需有足夠筆數，建議 >= 300 筆。
- 料號隔離測試集需能執行。

### Step 8：Baseline 訓練與評估

建立 baseline：

- Global Median。
- Category Median。
- Category + Material Median。
- Similar Spec Median。

輸出：

- 每個 baseline 的 MAE、RMSE、MAPE、Median APE、Within ±30%、Within ±50%。

完成條件：

- Category Median baseline 必須作為 ML 模型比較基準。

### Step 9：ML 模型訓練

訓練模型：

- Random Forest。
- LightGBM。
- XGBoost。

建議實驗組：

| 實驗 | 特徵 |
|---|---|
| A | 結構化規格特徵 |
| B | 結構化規格 + 廠商 |
| C | 結構化規格 + 文字 TF-IDF |
| D | 結構化規格 + embedding |

注意：

- 預測目標建議使用 `log1p(單價)`。
- 評估時需轉回原始價格。

完成條件：

- 至少有一個 ML 模型在時間切分下優於 baseline。

### Step 10：相似案例檢索

建立相似案例方法：

- 結構化特徵相似度。
- 品名 embedding 相似度。
- 混合相似度。

輸出每筆測試資料的：

- Top 5 相似案例。
- 相似案例單價。
- 相似案例日期。
- 相似案例供應商。
- 規格差異。

完成條件：

- 抽樣 30 筆，請業務或採購人員判斷相似案例是否合理。
- 合理率需 >= 70%，才建議進 MVP。

### Step 11：模型解釋

輸出：

- Feature importance。
- 每類品項模型表現。
- 高誤差案例分析。
- 高價品項是否常被低估。
- 模型不適用情境。

完成條件：

- 能說明模型為什麼預測高或低。
- 能列出哪些案例應人工覆核。

### Step 12：PoC 結論判斷

依據 Go / Watch / No-Go 表，輸出結論：

- 建議進 MVP。
- 建議補資料後再做第二輪 PoC。
- 不建議繼續。

## 7. 最終決策規則

### 建議進入 MVP 的條件

同時滿足以下條件，才建議進入 MVP：

1. 清洗後可用資料 >= 2,000 筆。
2. 尺寸、材質、板厚解析率皆 >= 70%。
3. ML 模型在時間切分測試集上明顯優於 Category Median baseline。
4. Median APE <= 35%，或相對 baseline 改善 >= 20%。
5. Within ±50% >= 65%。
6. 相似案例業務接受率 >= 70%。
7. 高價品項能被標示為低信心或需人工審核。

### 建議補資料後再評估的條件

符合以下任一情況，建議補資料再做第二輪 PoC：

1. 模型有改善，但 Median APE 仍在 35%-50%。
2. Within ±30% 低於 45%，但 Within ±50% 有明顯改善。
3. 高價品項錯誤大，但一般規格品表現可接受。
4. 品名解析足夠，但缺少 BOM、圖面、附件、品牌、交期、MOQ。
5. 業務認為相似案例方向正確，但資料不足。

### 建議暫停或不繼續的條件

符合以下任一情況，建議暫停：

1. ML 模型無法優於 Category Median baseline。
2. 時間切分與料號隔離切分表現都很差。
3. 品名解析率不足，且客戶無法補規格欄位。
4. 相似案例業務接受率 < 50%。
5. 客戶期待精準報價，但資料只能支撐粗略區間。

## 8. PoC 產出文件

PoC 完成後，應產出以下文件：

1. `PoC_Colab_Notebook.ipynb`
2. `資料品質檢查報告.md`
3. `品名解析規則與成功率報告.md`
4. `模型評估報告.md`
5. `相似案例檢索範例.xlsx`
6. `高誤差案例分析.xlsx`
7. `Go_No-Go_決策建議.md`

## 9. PoC 預估時程

| 週次 | 工作內容 | 產出 |
|---|---|---|
| 第 1 週 | 資料讀取、品質檢查、清洗規則 | 資料品質報告 |
| 第 2 週 | 品名解析、特徵工程、EDA | 特徵工程報告 |
| 第 3 週 | baseline、ML 模型、相似案例檢索 | 初版模型結果 |
| 第 4 週 | 評估、誤差分析、業務審查、Go/No-Go | PoC 結論報告 |

## 10. 決策者摘要

本 PoC 應用 Google Colab 快速驗證資料價值與技術可行性。成功標準不是模型能否給出一個看似精準的價格，而是模型是否能在時間外測試資料上明顯優於簡單歷史中位數 baseline，並提供採購人員可理解的價格區間與相似案例。

若模型能達到 `Median APE <= 35%`、`Within ±50% >= 65%`、相似案例業務接受率 `>= 70%`，且明顯優於 Category Median baseline，建議進入 MVP。

若模型只能提供粗略參考，但無法穩定優於 baseline，則應先補充 BOM、圖面、附件、品牌、交期、MOQ、議價紀錄等資料後，再進行第二輪 PoC。

若模型無法優於 baseline，或相似案例不被業務接受，則不建議繼續投入正式系統開發。
